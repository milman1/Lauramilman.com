<section class="page-404">
  <div class="page-404__inner">
    <span class="page-404__eyebrow">404</span>
    <h1 class="page-404__title">Page Not Found</h1>
    <p class="page-404__description">
      This piece is no longer available. Explore our current fine jewelry or certified natural diamonds.
    </p>
    <div class="page-404__actions">
      <a href="{{ routes.all_products_collection_url }}" class="btn btn--primary page-404__btn">Shop Fine Jewelry</a>
      <a href="/collections/natural-diamonds" class="page-404__secondary-btn">Explore Natural Diamonds</a>
    </div>
  </div>
</section>

<style>
  .page-404 {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 120px 40px;
  }
  .page-404__inner {
    max-width: 480px;
  }
  .page-404__eyebrow {
    display: block;
    font-family: var(--font-body);
    font-size: 11px;
    letter-spacing: 5px;
    text-transform: uppercase;
    color: var(--gold, #C9A050);
    margin-bottom: 24px;
  }
  .page-404__title {
    font-family: var(--font-heading);
    font-size: 52px;
    color: var(--text-dark, #2C1810);
    font-weight: 400;
    margin: 0 0 24px;
  }
  .page-404__description {
    font-family: var(--font-body);
    font-size: 15px;
    line-height: 1.8;
    color: var(--text-muted, #8A8070);
    font-weight: 300;
    margin-bottom: 40px;
  }
  .page-404__btn {
    display: inline-block;
    padding: 16px 48px;
    background: var(--gold, #C9A050);
    font-family: var(--font-body);
    font-size: 12px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--warm-white, #FDFAF6);
    text-decoration: none;
    transition: background 0.4s;
  }
  .page-404__btn:hover {
    background: var(--gold-light, #E2C27A);
  }
  .page-404__actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
  }
  .page-404__secondary-btn {
    font-family: var(--font-body);
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--text-dark, #2C1810);
    text-decoration: underline;
    text-underline-offset: 5px;
  }
  .page-404__secondary-btn:hover {
    color: var(--gold, #C9A050);
  }
  @media screen and (max-width: 749px) {
    .page-404 {
      min-height: 65vh;
      padding: 88px 24px;
    }
    .page-404__title {
      font-size: 42px;
    }
  }
</style>
