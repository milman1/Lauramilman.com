# Task: Source every Jacob & Co. listing from Bucherer and Exquisite Timepieces
- Issue: https://github.com/milman1/Lauramilman.com/issues/95
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: Per `AGENTS.md` recipe G2 and the "Jacob & Co. sourcing" bullet in section 4, source every Jacob & Co. reference each site lists, starting from the two URLs in `AGENTS.md` section 4: https://www.bucherer.com/us/en/watches/jacob-co/ and https://www.exquisitetimepieces.com/collections/jacob-co-epic-x. The work is split per the playbook: a Claude Sonnet 5 worker reads the Exquisite Timepieces `products.json` feed through Firecrawl (recipe G2) and writes those rows into `docs/suppliers/jacob-co-sourcing-2026-09.csv` first; Astra then reads Bucherer in the browser session (the sandbox cannot reach bucherer.com per `AGENTS.md` section 7 — this is an egress restriction on this environment, not a claim that Bucherer's pages require client-side rendering), fills the Bucherer columns in the same CSV, and cross-references by reference number. The CSV's exact header is `reference,collection,case_size_mm,case_material,dial,strap_or_bracelet,condition,price_bucherer,price_exquisite,price_lower_source,image_urls,source_url_bucherer,source_url_exquisite`. Price rule (merchant decision 2026-09-10): record each vendor's list price exactly as shown in its own column; when a reference appears on only one site, leave the other site's price column blank; `price_lower_source` names which site's price is lower (`bucherer` or `exquisite`) or, when only one site lists the reference, that site. Downstream, a Claude worker prices the product at the lower list price per `AGENTS.md` section 2a. Neither Bucherer's name nor Exquisite Timepieces' name goes anywhere on the storefront, per `AGENTS.md` rule 1's supplier-name discipline extended to these two source retailers. Acceptance: one row per reference (no duplicate reference rows even when a reference appears on both sites), the header matches exactly, and every price field present is a number. Independent check: a Claude worker (Sonnet 5) re-reads the result with a fresh query or a fresh page load and records the count in the work log before Status changes to done.
- Scope (files / live records): Live scope is read-only browsing of https://www.bucherer.com/us/en/watches/jacob-co/ (Astra, browser) and https://www.exquisitetimepieces.com/collections/jacob-co-epic-x (Claude Sonnet 5 worker, Firecrawl, via `/products.json`). Output is one file, `docs/suppliers/jacob-co-sourcing-2026-09.csv`, in this repository — the Sonnet 5 worker writes the Exquisite Timepieces rows first, Astra edits the same file to add the Bucherer columns and merge duplicate references. No Shopify write; product creation is a separate, later step done by a Claude worker under recipe G2, pricing at the lower of the two list prices.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: A Claude Sonnet 5 worker reads every product on `https://www.exquisitetimepieces.com/collections/jacob-co-epic-x/products.json` through Firecrawl and writes one row per reference to `docs/suppliers/jacob-co-sourcing-2026-09.csv` with the header above: `reference` (from the product's model/reference field or SKU), `collection`, `case_size_mm`, `case_material`, `dial`, `strap_or_bracelet`, `condition` (new or pre-owned), `price_exquisite` (list price as shown), `image_urls`, `source_url_exquisite`; leave `price_bucherer` and `source_url_bucherer` blank at this stage. Once that pass is committed, Astra opens https://www.bucherer.com/us/en/watches/jacob-co/ in the browser, pages through every Jacob & Co. listing, and opens each product page to record the same fields (case size and material, dial, strap or bracelet, condition, list price, image URLs, and the product page URL). For each Bucherer reference: if it already has a row from Exquisite Timepieces, fill in `price_bucherer` and `source_url_bucherer` on that same row and set `price_lower_source` to whichever of `price_bucherer`/`price_exquisite` is lower; if it is Bucherer-only, add a new row with `price_exquisite` and `source_url_exquisite` left blank and `price_lower_source` = `bucherer`. Never write "Bucherer" or "Exquisite Timepieces" into `collection`, `dial`, or any field that could reach the storefront — this file is a sourcing record only, not storefront copy; the `source_url_*` columns are the only place either domain appears, and those columns never leave this file. When the CSV is complete, commit it on a branch, open a pull request, and merge it to main; the handoff is not done until it is on main.
- Blockers / decisions needed: None known yet. If Bucherer blocks the browser session (bot detection, region gate), record that as a blocker with what was captured before the block, rather than guessing at the remaining listings.
- Exact next action: Have a Claude Sonnet 5 worker open `https://www.exquisitetimepieces.com/collections/jacob-co-epic-x/products.json` through Firecrawl and begin listing every Jacob & Co. reference and its fields into `docs/suppliers/jacob-co-sourcing-2026-09.csv`; once that pass is committed, Astra moves to Bucherer for the same collection.
- Supporting artifacts (durable location, access, expiry if applicable): Output `docs/suppliers/jacob-co-sourcing-2026-09.csv` is committed on its own branch, then merged to `main` via pull request; link the branch name, PR, and commit hash here once pushed. After this task, a Claude worker creates DRAFT products under recipe G2 in `AGENTS.md`, pricing each at the lower of `price_bucherer`/`price_exquisite`.

## Work log
- 2026-09-10: Handoff file created. No sourcing data collected yet.
