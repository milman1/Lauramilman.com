# Task: Find wholesale cost for the 94 SKU-bearing Peaceful Diamonds lab-grown SKUs in a merchant-run browser session
- Issue: https://github.com/milman1/Lauramilman.com/issues/94
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: The merchant decided on 2026-09-10 that Astra finds the wholesale cost of the Peaceful Diamonds lab-grown jewelry pieces so the cost x 4 rule can run (`AGENTS.md` section 2a, `LAB_GROWN_JEWELRY` row). Input is `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` (handle, title, SKU, current price for 94 of the 102 active Shopify products with vendor Peaceful Diamonds, SKUs of the form `BC14...`, `NK14...`), committed at that path with 94 rows (one row per SKU-bearing variant; the remaining 8 of the 102 active Peaceful Diamonds products have no SKU and are excluded from this export — see the 2026-09-10 Work log entries below, unchanged from the original export). Cost is never committed to this public repository (rule 2, section 8): Astra saves a cost CSV — columns `sku,handle,vendor_site,cost,retail,error` — to the merchant's Google Drive folder "LMNY supplier costs" (the merchant creates and shares the folder; name the file `94-peaceful-diamonds-costs-2026-09.csv` and note the file name, never its contents, in the work log), and commits to this repository only the public companion file `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` with columns `sku,handle,vendor_site,retail,cost_found,error` (no cost column; `cost_found` is `yes` or `no`). `retail` = round(cost x 4); it is public because it is the storefront price, not the cost. A Claude worker later reads the Drive cost file through the Google Drive connector and writes cost into Shopify Cost per item, which is the approved system for cost. Acceptance: both files have 94 rows; every row has either a cost/retail pair (`cost_found` = `yes` in the public file) or an explicit `error` reason with `cost_found` = `no` (never both blank, never a guessed cost); the two files' `sku`, `handle`, `vendor_site`, `retail`, and `error` columns match row for row; no credential and no cost value appears in this repository, this handoff, or a screenshot. Independent check: a Claude worker (Sonnet 5) re-reads the result with a fresh query or a fresh page load and records the count in the work log before Status changes to done.
- Scope (files / live records): Read `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` in this repository (committed, 94 rows; do not query Shopify for the SKU list). Live scope is web search to identify the vendor, then — only after the merchant confirms the candidate vendor named in the work log — that vendor's trade portal in a merchant-run browser session. Outputs: a cost file in the merchant's Google Drive folder "LMNY supplier costs" (never in this repository), and a public companion file `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` committed on a branch in this repository. No Shopify write of any kind; the reprice is a later step done by a Claude worker under rules 4 and 5.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: Astra names the candidate vendor and its portal URL in the work log and stops for the merchant to confirm before any login. To reach that point, research the `BC14...` / `NK14...` SKU pattern (product photography, packaging, site language, or a search for "Peaceful Diamonds" plus known lab-grown jewelry manufacturers/wholesalers) to identify the underlying manufacturer or wholesaler, and write the candidate name and portal URL into the work log. If the wholesaler this points to turns out not to be Peaceful Diamonds, section 2a's new-supplier row applies: a Claude worker adds that supplier's own row to `AGENTS.md` section 2a and its own pricing constant to `config/pricing.ts` before any reprice runs, since the cost x 4 rule is specific to the `LAB_GROWN_JEWELRY` row and must not be borrowed for a different supplier. Once the merchant confirms the vendor, in the merchant's browser session the merchant logs in to that vendor's trade portal (Astra never types or stores the credentials). For each of the 94 SKUs, find the matching item in the portal and read its wholesale cost; compute `retail` = round(cost x 4). Write one row per SKU to the Drive cost file `94-peaceful-diamonds-costs-2026-09.csv` (columns `sku,handle,vendor_site,cost,retail,error`) in the merchant's Google Drive folder "LMNY supplier costs", and the matching row to the public companion `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` (columns `sku,handle,vendor_site,retail,cost_found,error`). Where a SKU cannot be matched in the portal (discontinued, different numbering, not carried by this vendor), leave `cost`/`retail` blank in the Drive file, `cost_found` = `no` in the public file, and record a plain-English reason in `error` in both. Never include a customer name, address, or order detail anywhere in either output, even if the portal surfaces one incidentally. When all 94 rows are written in both files, commit the public companion file on a branch, open a pull request, and merge it to main; the handoff is not done until it is on main.
- Blockers / decisions needed: None on the input file. If the vendor cannot be identified with confidence, stop and ask the merchant to confirm or name the vendor rather than guessing and logging in to the wrong portal — this is the same stop rule as above, restated: never log in before the merchant confirms the vendor named in the work log.
- Exact next action: `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` is committed at that path with 94 rows; begin vendor identification research per the goal above and write the candidate vendor and portal URL into the work log for the merchant to confirm.
- Supporting artifacts (durable location, access, expiry if applicable): `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` (committed, 94-row input). Cost output `94-peaceful-diamonds-costs-2026-09.csv` lives in the merchant's Google Drive folder "LMNY supplier costs" (durable, merchant-owned; note access here once shared). Public companion output `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` is committed on its own branch, then merged to `main` via pull request; link the branch name, PR, and commit hash here once pushed. After this task, a Claude worker reads the Drive cost file through the Google Drive connector, writes cost into Shopify Cost per item, and applies the reprice under `AGENTS.md` rules 4 and 5.

## Work log
- 2026-09-10: Handoff file created. `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` is committed at that path with 94 rows.
- 2026-09-10 Claude worker: exported 94 active SKUs to docs/suppliers/peaceful-diamonds-skus-2026-09.csv.
