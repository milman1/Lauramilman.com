# Task: Source every Jacob & Co. listing from Bucherer and Exquisite Timepieces
- Issue:
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: Per `AGENTS.md` recipe G2 and the "Jacob & Co. sourcing" bullet in section 4, read every Jacob & Co. listing on https://www.exquisitetimepieces.com/collections/jacob-co-epic-x (a Shopify store; its `/products.json` feed is allowed) and https://www.bucherer.com/us/en/watches/jacob-co/ (a custom site, browser only). For each listing record reference, collection, case size and material, dial, strap or bracelet, the list price as shown, condition (new or pre-owned), image URLs, and the source URL. Write `docs/suppliers/jacob-co-sourcing-2026-09.csv` with those columns. Price rule (merchant decision 2026-09-10): record the vendor's list price exactly as shown; when the same reference appears on both sites, record both prices in the row and mark which one is lower. Neither Bucherer's name nor Exquisite Timepieces' name goes anywhere on the storefront, per `AGENTS.md` rule 1's supplier-name discipline extended to these two source retailers. Acceptance: one row per reference (no duplicate reference rows even when a reference appears on both sites), no duplicate rows otherwise, and every price field is a number.
- Scope (files / live records): Live scope is read-only browsing of the two named retailer URLs (and Exquisite Timepieces' `/products.json` feed under its `jacob-co-epic-x` collection). Output is a new file, `docs/suppliers/jacob-co-sourcing-2026-09.csv`, in this repository. No Shopify write; product creation is a separate, later step done by a Claude worker under recipe G2.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: Exquisite Timepieces is a Shopify store, so its Jacob & Co. catalog can be read from `https://www.exquisitetimepieces.com/collections/jacob-co-epic-x/products.json` (this part could in principle be a Sonnet 5/Terra job through Firecrawl per the routing table, but is included here so Astra can cross-reference references against Bucherer in one pass); for each product, record reference, collection, case size and material, dial, strap or bracelet, list price, condition, image URLs, and the product page URL as the source. For Bucherer, visit https://www.bucherer.com/us/en/watches/jacob-co/ in the browser, page through every Jacob & Co. listing, and open each product page to record the same fields; Bucherer's product pages are rendered client-side and are the reason this is a browser task rather than a scrape. Once both sites are read, merge by reference: when a reference appears on only one site, write one row from that site; when it appears on both, write one row carrying both prices and a note of which is lower, per the merchant's price rule. Do not write "Bucherer" or "Exquisite Timepieces" into any field that could reach the storefront (title, body, tags, alt text) — this file is a sourcing record only, not storefront copy. When the CSV is complete, commit it on a branch and record the branch name here.
- Blockers / decisions needed: None known yet. If Bucherer blocks the browser session (bot detection, region gate), record that as a blocker with what was captured before the block, rather than guessing at the remaining listings.
- Exact next action: Open https://www.exquisitetimepieces.com/collections/jacob-co-epic-x/products.json in the browser session and begin listing every Jacob & Co. reference and its fields, then move to Bucherer for the same collection.
- Supporting artifacts (durable location, access, expiry if applicable): Output `docs/suppliers/jacob-co-sourcing-2026-09.csv` is committed on its own branch when done; link the branch name and commit hash here once pushed. After this task, a Claude worker creates DRAFT products under recipe G2 in `AGENTS.md`.

## Work log
- 2026-09-10: Handoff file created. No sourcing data collected yet.
