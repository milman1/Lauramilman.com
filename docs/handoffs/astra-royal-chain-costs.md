# Task: Read Royal Chain wholesale cost for the 21-chain shortlist in a merchant-run browser session
- Issue:
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: The merchant decided on 2026-09-10 that Astra reads wholesale cost for every row of `docs/suppliers/royal-chain-shortlist-2026-09.csv` (21 chains) directly in a browser session where the merchant is logged in to their own Royal Chain trade account, instead of running the GitHub Actions cost job. For each item's URL, record the wholesale cost, every length and karat offered with its price, the item number, and the image URLs shown on the page. Write the result as `docs/suppliers/royal-chain-costs-2026-09.csv` in the exact format the GitHub Actions cost job would have produced: columns `item_number,url,cost,retail,price_text,lengths,error`, where `retail` = cost x 3 rounded up to the nearest $5 (`AGENTS.md` section 2a, Royal Chain row, and recipe H step 4). Acceptance: the file has 21 rows (one per shortlist item); every `cost` is a positive number, or the row's `error` column explains why it could not be read and `cost`/`retail` are left blank for that row; every `retail` equals `ceil(cost * 3 / 5) * 5`. No Royal Chain credentials appear anywhere in the CSV, the work log, or any screenshot taken during the session.
- Scope (files / live records): Read `docs/suppliers/royal-chain-shortlist-2026-09.csv` (21 rows) in this repository for the item URLs and item numbers. Live scope is the merchant's own Royal Chain trade account in a browser, visiting only the 21 URLs listed in that shortlist. Output is a new file, `docs/suppliers/royal-chain-costs-2026-09.csv`, committed on a branch (not `main`) in this repository. No Shopify write of any kind; product creation is a separate, later step done by a Claude worker.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: In the merchant's browser session, log in to the Royal Chain trade account (the merchant enters credentials themselves; Astra never types or stores them). For each of the 21 rows in `docs/suppliers/royal-chain-shortlist-2026-09.csv`, open the row's `url`, read the wholesale cost shown once logged in, and record every length/karat variant offered on that page along with its price (`price_text` and `lengths`), the item number (should match the shortlist's `item_number` column; note if it differs), and the image URLs shown. Compute `retail` as cost x 3, rounded up to the nearest $5. Write one row per item to `docs/suppliers/royal-chain-costs-2026-09.csv` with header `item_number,url,cost,retail,price_text,lengths,error`. If a page fails to load, shows no price, or the item is out of stock, leave `cost` and `retail` blank and put a plain-English reason in `error`; never guess a cost. When all 21 rows are written, commit the file on a new branch (never `main` directly, per `AGENTS.md` rule 11) with a plain commit message, and record the branch name in this handoff. Do not include the trade login URL, username, or any credential fragment in the CSV, this handoff, or a screenshot; crop or redact the browser chrome if a screenshot must show the account is logged in.
- Blockers / decisions needed: None known yet. If Royal Chain requires a CAPTCHA or blocks the session mid-run, note that as a blocker and record which item numbers were completed before the block, so a resumed session does not redo them (idempotent work file, per `AGENTS.md` rule 5).
- Exact next action: In a merchant-run browser session, log in to the Royal Chain trade account, then open the first URL in `docs/suppliers/royal-chain-shortlist-2026-09.csv` and begin recording cost, lengths, karats, item number, and image URLs.
- Supporting artifacts (durable location, access, expiry if applicable): `docs/suppliers/royal-chain-shortlist-2026-09.csv` (21-row input, committed in this repository). Output `docs/suppliers/royal-chain-costs-2026-09.csv` is committed on its own branch when done; link the branch name and commit hash in this handoff once pushed. After this task, a Claude worker creates the 21 products as DRAFT under recipe H step 4 in `AGENTS.md`.

## Work log
- 2026-09-10: Handoff file created. No cost data read yet.
