# Task: Find wholesale cost for the 102 Peaceful Diamonds lab-grown SKUs in a merchant-run browser session
- Issue:
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: The merchant decided on 2026-09-10 that Astra finds the wholesale cost of the Peaceful Diamonds lab-grown jewelry pieces so the cost x 4 rule can run (`AGENTS.md` section 2a, `LAB_GROWN_JEWELRY` row). Input is `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` (handle, title, SKU, current price for the 102 active Shopify products with vendor Peaceful Diamonds, SKUs of the form `BC14...`, `NK14...`), committed at that path with 94 rows (one row per SKU-bearing variant; see Work log). Astra's job: identify the manufacturer or wholesaler behind the `BC14.../NK14...` SKU pattern by searching, log in to that vendor's trade portal with credentials the merchant types themselves in the browser session, read the wholesale cost per SKU, and write `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` with columns `sku,handle,vendor_site,cost,retail,error`, where `retail` = round(cost x 4). Acceptance: every one of the 102 SKUs has either a `cost` or an explicit `error` reason (never both blank, never a guessed cost); no credential and no customer data appears in the output file, this handoff, or a screenshot.
- Scope (files / live records): Read `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` in this repository (committed, 94 rows; do not query Shopify for the SKU list). Live scope is web search to identify the vendor, then that vendor's trade portal in a merchant-run browser session. Output is a new file, `docs/suppliers/peaceful-diamonds-costs-2026-09.csv`, in this repository. No Shopify write of any kind; the reprice is a later step done by a Claude worker under rules 4 and 5.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` is committed at that path with 94 rows. Research the `BC14...` / `NK14...` SKU pattern (product photography, packaging, site language, or a search for "Peaceful Diamonds" plus known lab-grown jewelry manufacturers/wholesalers) to identify the underlying manufacturer or wholesaler. In the merchant's browser session, the merchant logs in to that vendor's trade portal (Astra never types or stores the credentials). For each of the 102 SKUs, find the matching item in the portal and read its wholesale cost; compute `retail` = round(cost x 4). Write one row per SKU to `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` with header `sku,handle,vendor_site,cost,retail,error`. Where a SKU cannot be matched in the portal (discontinued, different numbering, not carried by this vendor), leave `cost` and `retail` blank and record a plain-English reason in `error`. Never include a customer name, address, or order detail anywhere in the output, even if the portal surfaces one incidentally. When all 102 rows are written, commit the file on a branch and record the branch name here.
- Blockers / decisions needed: None on the input file. If the vendor cannot be identified with confidence, stop and ask the merchant to confirm or name the vendor rather than guessing and logging in to the wrong portal.
- Exact next action: `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` is committed at that path with 94 rows; begin vendor identification research per the goal above.
- Supporting artifacts (durable location, access, expiry if applicable): `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` (committed, 94-row input). Output `docs/suppliers/peaceful-diamonds-costs-2026-09.csv` is committed on its own branch when done; link the branch name and commit hash here once pushed. After this task, a Claude worker applies the reprice under `AGENTS.md` rules 4 and 5.

## Work log
- 2026-09-10: Handoff file created. `docs/suppliers/peaceful-diamonds-skus-2026-09.csv` is committed at that path with 94 rows.
- 2026-09-10 Claude worker: exported 94 active SKUs to docs/suppliers/peaceful-diamonds-skus-2026-09.csv.
