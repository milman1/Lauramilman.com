# Task: End the 640 archived-watch eBay listings and disable them in Marketplace Connect
- Issue:
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: Remove the 640 archived-watch listings from eBay and stop Marketplace Connect from relisting them, without touching the 155 keeper SKUs. All 640 are already archived in Shopify (verified 2026-09-09); this task is eBay-side and Marketplace-Connect-side only, it does not touch Shopify product status. Acceptance: eBay Seller Hub's Active listings show none of the 640 SKUs in `docs/ebay/ebay_end_skus.csv` still active; all 155 SKUs in `docs/ebay/ebay_keeper_skus_unique.csv` are still active and untouched; the work log records a count of how many of the 640 were ended, how many were already gone, and confirms zero keeper SKUs were touched.
- Scope (files / live records): Read-only on `docs/ebay/CLAUDE_TAKEDOWN_HANDOFF.md`, `docs/ebay/batches/README.md`, `docs/ebay/batches/*`, `docs/ebay/ebay_end_skus.csv` (640 SKUs), and `docs/ebay/ebay_keeper_skus_unique.csv` (155 SKUs) in this repository. Live scope is eBay Seller Hub (Listings > Active, and Reports > Upload) for the merchant's eBay seller account, and the Marketplace Connect (Codisto) bulk eBay screen in Shopify admin for the `laura-milman` store. Do not touch Shopify product status, price, or any product outside these SKU lists.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: Follow `docs/ebay/CLAUDE_TAKEDOWN_HANDOFF.md`. Preferred path: in eBay Seller Hub, go to Listings > Active and export the CSV (Item ID plus SKU columns). Keep only rows whose SKU appears in `docs/ebay/ebay_end_skus.csv` (640 codes). Remove any row whose SKU appears in `docs/ebay/ebay_keeper_skus_unique.csv` (155 codes); never end those. Build an End listings CSV with columns `Action,Item number,EndingReason` (Action `End`, EndingReason `NotAvailable`) from the remaining Item IDs. Upload it in Seller Hub under Reports > Upload using the "End listings" template. Then open the Marketplace Connect bulk eBay screen at the Connect URL in the handoff doc, find the same 640 SKUs (by Code), and set each to Disabled, saving in batches of 8 to 10 and waiting for "Listings Saved" after each batch, so Connect does not relist them. If Seller Hub access is not available, fall back to the Marketplace-Connect-only path in the same handoff doc: Phase 1 disables the prefix-safe `P` and `U` (and any `M`) codes in `docs/ebay/batches/phase1_P_codes.txt` and `docs/ebay/batches/phase1_U_codes.txt`; Phase 2 works `docs/ebay/batches/batch_01_codes.txt` through `batch_13_codes.txt`, one exact Code searched and disabled at a time, never a prefix bulk-select for `T`, `RW`, or numeric codes. After either path, verify in Seller Hub that none of the 640 remain active and that all 155 keepers are still active, and write the final counts into the work log.
- Blockers / decisions needed: None known yet. If eBay Seller Hub login is not available in this session, note that and proceed with the Marketplace Connect fallback path instead of waiting.
- Exact next action: Log in to eBay Seller Hub (or Shopify admin for Marketplace Connect if Seller Hub is unavailable) and export or open the Active listings view to begin matching SKUs against `docs/ebay/ebay_end_skus.csv`.
- Supporting artifacts (durable location, access, expiry if applicable): `docs/ebay/CLAUDE_TAKEDOWN_HANDOFF.md`, `docs/ebay/batches/README.md`, `docs/ebay/batches/batch_01.csv` through `batch_13.csv` and their `_codes.txt` files, `docs/ebay/ebay_end_skus.csv` (640 rows), `docs/ebay/ebay_keeper_skus_unique.csv` (155 rows) — all committed in this repository under `docs/ebay/`.

## Work log
- 2026-09-10: Handoff file created. All 640 target SKUs are archived in Shopify already (verified 2026-09-09, per `docs/ebay/CLAUDE_TAKEDOWN_HANDOFF.md`); this task is eBay Seller Hub and Marketplace Connect only. No takedown work performed yet.
