# Task: Read Royal Chain wholesale cost for the 21-chain shortlist in a merchant-run browser session
- Issue: https://github.com/milman1/Lauramilman.com/issues/93
- Updated (UTC): 2026-09-10
- Owner / session: Astra (OpenAI GPT-6 Astra), browser session run by the merchant
- Status: planned
- Goal and acceptance criteria: The merchant decided on 2026-09-10 that the Astra browser session is the **primary** path for reading Royal Chain wholesale cost for every row of `docs/suppliers/royal-chain-shortlist-2026-09.csv` (21 chains), with the merchant logged in to their own Royal Chain trade account; the GitHub Actions "Royal Chain costs" workflow (recipe H step 3) is the **fallback** if the browser session is blocked. For each item's URL, record the wholesale cost and every length and karat offered with its price. Cost is never committed to this public repository (rule 2, section 8): Astra saves a cost CSV — columns `item_number,url,cost,retail,lengths_karats,error` — to the merchant's Google Drive folder "LMNY supplier costs" (the merchant creates and shares the folder; name the file `93-royal-chain-costs-2026-09.csv` and note the file name, never its contents, in the work log), and commits to this repository only the public companion file `docs/suppliers/royal-chain-costs-2026-09.csv` with columns `item_number,url,retail,price_read,lengths_karats,error` (no cost column; `price_read` is `yes` or `no`). `retail` = cost x 3 rounded up to the nearest $5 (`AGENTS.md` section 2a, Royal Chain row, and recipe H step 4); it is public because it is the storefront price, not the cost. A Claude worker later reads the Drive cost file through the Google Drive connector and writes cost into Shopify Cost per item, which is the approved system for cost. Acceptance: both files have 21 rows (one per shortlist item); every row has either a cost/retail pair or an explicit `error` reason and `price_read` of `no` (never a guessed cost); every `retail` equals `ceil(cost * 3 / 5) * 5`; the two files' `item_number`, `url`, `retail`, `lengths_karats`, and `error` columns match row for row. No Royal Chain credentials appear anywhere in either CSV, the work log, or any screenshot. Independent check: a Claude worker (Sonnet 5) re-reads the result with a fresh query or a fresh page load and records the count in the work log before Status changes to done.
- Scope (files / live records): Read `docs/suppliers/royal-chain-shortlist-2026-09.csv` (21 rows) in this repository for the item URLs and item numbers. Live scope is the merchant's own Royal Chain trade account in a browser, visiting only the 21 URLs listed in that shortlist. Outputs: a cost file in the merchant's Google Drive folder "LMNY supplier costs" (never in this repository), and a public companion file `docs/suppliers/royal-chain-costs-2026-09.csv` committed on a branch (not `main`) in this repository. No Shopify write of any kind; product creation is a separate, later step done by a Claude worker.
- Branch / commit / PR:
- Completed steps:
- Verification (checks, outcomes, evidence links, unverified items):
- Deployment / live state:
- Remaining work: In the merchant's browser session, log in to the Royal Chain trade account (the merchant enters credentials themselves; Astra never types or stores them). For each of the 21 rows in `docs/suppliers/royal-chain-shortlist-2026-09.csv`, open the row's `url` and read the wholesale cost shown once logged in, along with every length/karat variant offered on that page and its price. Encode that as `lengths_karats`, one segment per option in the style `14K:16in=$x;14K:18in=$y` (metal/karat colon length, equals its price, segments separated by `;`); use retail prices in the public file's `lengths_karats` and cost prices in the Drive file's `lengths_karats`, keeping the segment order the same across both so a reader can line them up without the raw cost ever appearing here. Compute `retail` as cost x 3, rounded up to the nearest $5. Write one row per item to the Drive cost file `93-royal-chain-costs-2026-09.csv` (columns `item_number,url,cost,retail,lengths_karats,error`) in the merchant's Google Drive folder "LMNY supplier costs", and the matching row to the public companion `docs/suppliers/royal-chain-costs-2026-09.csv` (columns `item_number,url,retail,price_read,lengths_karats,error`, `price_read` = `yes` when a cost was read, `no` when it was not). If a page fails to load, shows no price, or the item is out of stock, leave `cost`/`retail` blank in the Drive file, `price_read` = `no` in the public file, and put a plain-English reason in `error` in both; never guess a cost. When all 21 rows are written in both files, commit the public companion file on a branch, open a pull request, and merge it to main; the handoff is not done until it is on main. Note the Drive file name in the work log (never its contents). Do not include the trade login URL, username, or any credential fragment in either CSV, this handoff, or a screenshot; crop or redact the browser chrome if a screenshot must show the account is logged in, and save any such screenshot to the same Google Drive folder, named `93-royal-chain-costs-<step>.png`, with the file name noted in the work log. If the browser session is blocked (CAPTCHA, login failure), fall back to the GitHub Actions "Royal Chain costs" workflow per recipe H step 3 and record in the work log that the fallback was used and why.
- Blockers / decisions needed: None known yet. If Royal Chain requires a CAPTCHA or blocks the session mid-run, note that as a blocker, record which item numbers were completed before the block (idempotent work file, per `AGENTS.md` rule 5), and fall back to the GitHub Actions job per the primary/fallback rule above.
- Exact next action: In a merchant-run browser session, log in to the Royal Chain trade account, then open the first URL in `docs/suppliers/royal-chain-shortlist-2026-09.csv` and begin recording cost, retail, and the lengths/karats breakdown.
- Supporting artifacts (durable location, access, expiry if applicable): `docs/suppliers/royal-chain-shortlist-2026-09.csv` (21-row input, committed in this repository, already carries `image_url` per row so no image URLs need to be recorded again here). Cost output `93-royal-chain-costs-2026-09.csv` lives in the merchant's Google Drive folder "LMNY supplier costs" (durable, merchant-owned; note access here once shared). Public companion output `docs/suppliers/royal-chain-costs-2026-09.csv` is committed on its own branch, then merged to `main` via pull request; link the branch name, PR, and commit hash in this handoff once pushed. After this task, a Claude worker reads the Drive cost file through the Google Drive connector, writes cost into Shopify Cost per item, and creates the 21 products as DRAFT under recipe H step 4 in `AGENTS.md`.

## Work log
- 2026-09-10: Handoff file created. No cost data read yet.
